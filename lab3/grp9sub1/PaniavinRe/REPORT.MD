# Лабораторная 3 — Потоки, синхронизация и гонки данных

## Цель
- Понять модель потоков (TID, Threads) и где их смотреть в системе.
- На практике воспроизвести race condition и корректно её устранить.
- Освоить базовые примитивы: `threading.Lock`, `threading.Condition` и механизмы синхронизации.
- Сопоставить корректность и производительность при разной синхронизации.

## Ход работы (Вариант 2)

### Подготовка
Все программы реализованы на Python 3

### А) Гонка данных и устранение (mutex vs atomic)

Реализована программа `thread_race.py`, где N потоков суммарно выполняют M инкрементов общего счётчика. Сравниваются два корректных способа синхронизации:

- **Mutex** - с использованием `threading.Lock`
- **Atomic** - с использованием атомарных операций Python

#### Тестирование производительности

Запуск отдельных тестов:
python3 thread_race.py 4 1000000 mutex
python3 thread_race.py 4 1000000 atomic

### Результаты
mode=mutex threads=4 iters_per_thread=1000000 expected=4000000 actual=4000000 time_ms=528
mode=atomic threads=4 iters_per_thread=1000000 expected=4000000 actual=4000000 time_ms=133

### Автоматическое тестирование с различными параметрами:
python3 bench_race.py

### Результаты бенчмарка:
Summary (avg time_ms over 5 runs)
mode     N      M(per-thread) avg_time_ms 
mutex    1      2000000      255.80      
mutex    2      2000000      521.00      
mutex    4      2000000      1048.80     
mutex    8      2000000      2041.40     
atomic   1      2000000      61.40       
atomic   2      2000000      127.00      
atomic   4      2000000      251.20      
atomic   8      2000000      498.40

### Анализ результатов
Оба варианта корректны - все значения actual == expected. Однако наблюдается значительная разница в производительности:

-Atomic быстрее в ~4.2 раза при 4 потоках (133ms vs 528ms)

-С ростом числа потоков разрыв сохраняется: atomic в ~4.1 раза быстрее при 8 потоках

-Mutex работает медленнее из-за накладных расходов на блокировку/разблокировку

-Atomic операции в Python эффективнее для простых инкрементов

### B) Producer-Consumer (bounded buffer, condition variables)

Реализован bounded buffer на Python с использованием threading.Condition. Буфер использует:

-threading.Lock для защиты критической секции

-threading.Condition для синхронизации производителей и потребителей

-Кольцевую очередь фиксированного размера

### Тестирование корректности
python3 prodcons.py -P 2 -C 2 -N 100000 -B 64

### Результат:
[prodcons] P=2 C=2 N=100000 B=64 produced=100000 consumed=100000 sum=152499950000
[OK] Sum check passed: 152499950000

### Программа работает корректно:

-Все 100000 элементов произведены и потреблены

-Сумма значений соответствует ожидаемой

-Отсутствуют потери данных и гонки

### C) Где видны потоки

### Для демонстрации потоков в системе запустим программу в фоновом режиме:
cd src
python3 prodcons.py -P 2 -C 2 -N 20000000 -B 64 & PID=$!

### Просмотр информации о потоках:
echo "[PID] $PID"
ps -L -p "$PID" -o pid,tid,psr,pcpu,stat,comm | head -n 10
cat /proc/"$PID"/status | grep Threads
ls -l /proc/"$PID"/task | head -n 10
wait "$PID"

### Ожидаемый вывод (пример):
[PID] 12345
    PID     TID PSR %CPU STAT COMMAND
  12345   12345   1  0.1 Sl   python3
  12345   12346   2 25.3 R    python3  
  12345   12347   3 24.8 R    python3
  12345   12348   4 25.1 R    python3
  12345   12349   0 24.9 R    python3
  
Threads:        5
total 0
dr-xr-xr-x 7 user user 0 Oct  6 12:00 12345
dr-xr-xr-x 7 user user 0 Oct  6 12:00 12346
dr-xr-xr-x 7 user user 0 Oct  6 12:00 12347
dr-xr-xr-x 7 user user 0 Oct  6 12:00 12348
dr-xr-xr-x 7 user user 0 Oct  6 12:00 12349

### Анализ:

-В процессе 5 потоков (главный + 2 производителя + 2 потребителя)

-Главный поток в состоянии Sleeping (Sl), рабочие потоки в состоянии Running (R)

-Потоки распределены по разным ядрам процессора (PSR)

-Каждый поток имеет свой TID (Thread ID)

-В /proc/$PID/task видны все потоки процесса

### Ответы на вопросы
1. Чем поток отличается от процесса? Где увидеть поток в /proc и ps?

Поток - это легковесная единица выполнения внутри процесса, разделяющая его память и ресурсы. Процесс - это изолированная единица выполнения со своей памятью.

Потоки можно увидеть:

-В ps -L -p PID - показываются все TID (Thread IDs)

-В /proc/PID/task/ - каждая подпапка соответствует потоку

-В /proc/PID/status в строке Threads:

2. Что такое race condition и почему volatile не решает проблему корректности?

Race condition возникает когда несколько потоков одновременно обращаются к общим данным и хотя бы один поток изменяет эти данные.

volatile в Python не существует как ключевое слово, но даже в языках где он есть, он только гарантирует чтение из основной памяти, но не атомарность операций. Для корректной синхронизации нужны мьютексы, атомарные операции или другие примитивы.

3. Когда выбирать mutex/condvar, когда semaphore, когда атомики?

-Atomic - для простых операций (инкремент, декремент, CAS)

-Mutex - для защиты сложных критических секций

-Condition Variable - для координации между потоками (producer-consumer)

-Semaphore - для ограничения доступа к ресурсам

4. Почему синхронизация замедляет выполнение и как уменьшать contention?

Синхронизация замедляет выполнение из-за:

-Накладных расходов на блокировки

-Ожидания доступа к ресурсам

-Переключения контекста

Способы уменьшения contention:

-Использовать атомарные операции вместо мьютексов где возможно

-Уменьшать время удержания блокировок

-Использовать lock-free структуры данных

-Разделять данные между потоками

5. Что такое ложное совместное использование (false sharing) и как его избежать?

False sharing возникает когда несвязанные данные, часто используемые разными потоками, попадают в одну кэш-линию. При изменении одного потоками, другие потоки вынуждены перечитывать кэш-линию.

Способы избежания:

-Выравнивание данных по границам кэш-линий

-Размещение часто изменяемых данных отдельно

-Использование локальных копий данных в потоках

### Выводы

-Оба способа синхронизации (mutex и atomic) обеспечивают корректность

-Atomic операции значительно быстрее для простых инкрементов

-Producer-Consumer паттерн корректно работает с condition variables

-Потоки видны в системе как отдельные TID с общей памятью процесса
