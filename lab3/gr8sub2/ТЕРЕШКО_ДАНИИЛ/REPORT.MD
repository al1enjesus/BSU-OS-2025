# Лабораторная работа №3 — Потоки, синхронизация и гонки данных

## Цель работы

- Понять модель потоков (TID, Threads) и где их смотреть в системе.
- На практике воспроизвести race condition и корректно её устранить.
- Освоить базовые примитивы: pthread_mutex_t, pthread_cond_t и/или POSIX sem_t.
- Сопоставить корректность и производительность при разной синхронизации.

## Среда выполнения
- Ubuntu 24.04.3 LTS на VM VirtualBox

---

## Вариант 1

---

## Ход работы

### A) Гонка данных и устранение (инкремент счётчика)
Цель: Реализовать программу, где N потоков суммарно выполняют M инкрементов общего счётчика.

Шаги/Решения:
   - Реализовать инкремент счётчика в трёх режимах: без синхронизации, с мьютексом, с атомиками
   - Продемонстрировать гонку данных и устранить её
   - Сравнить производительность подходов

Код:
    Код программы представлен в файле thread_race.c, сборка и запуск файла осуществляется при помощи Makefile, конкретные параметры для запуска thread_race.c демонстрируются ниже:

Команды:
```bash
# Без синхронизации (должны видеть гонку данных)
./thread_race 1 1000000 unsync
./thread_race 2 1000000 unsync
./thread_race 4 1000000 unsync
./thread_race 8 1000000 unsync

# С мьютексом (должно быть корректно)
./thread_race 1 1000000 mutex
./thread_race 2 1000000 mutex
./thread_race 4 1000000 mutex
./thread_race 8 1000000 mutex

# С атомиками (должно быть корректно)
./thread_race 1 1000000 atomic
./thread_race 2 1000000 atomic
./thread_race 4 1000000 atomic
./thread_race 8 1000000 atomic
```

Результаты:
```bash
mode=unsync threads=1 iters_per_thread=1000000 expected=1000000 actual=1000000 time_ms=4
mode=unsync threads=2 iters_per_thread=1000000 expected=2000000 actual=1019372 time_ms=4
mode=unsync threads=4 iters_per_thread=1000000 expected=4000000 actual=2027229 time_ms=7
mode=unsync threads=8 iters_per_thread=1000000 expected=8000000 actual=3347545 time_ms=11

mode=mutex threads=1 iters_per_thread=1000000 expected=1000000 actual=1000000 time_ms=20
mode=mutex threads=2 iters_per_thread=1000000 expected=2000000 actual=2000000 time_ms=71
mode=mutex threads=4 iters_per_thread=1000000 expected=4000000 actual=4000000 time_ms=165
mode=mutex threads=8 iters_per_thread=1000000 expected=8000000 actual=8000000 time_ms=307

mode=atomic threads=1 iters_per_thread=1000000 expected=1000000 actual=1000000 time_ms=11
mode=atomic threads=2 iters_per_thread=1000000 expected=2000000 actual=2000000 time_ms=21
mode=atomic threads=4 iters_per_thread=1000000 expected=4000000 actual=4000000 time_ms=42
mode=atomic threads=8 iters_per_thread=1000000 expected=8000000 actual=8000000 time_ms=91
```
Выводы:
   Гонка данных подтвердилась - без синхронизации счетчик показывает некорректные значения. Оба способа синхронизации (мьютекс и атомики) устраняют проблему, но      атомики в 3-4 раза быстрее. Без синхронизации работает быстро, но с ошибками.
   
### B) Producer–Consumer (bounded buffer, mutex+condvar)
Цель: Реализуйте фиксированную кольцевую очередь. Несколько производителей генерируют элементы, несколько потребителей их извлекают.

Шаги/Решения:
   - Реализовать кольцевой буфер producer-consumer
   - Настроить синхронизацию через mutex + condvar
   - Обеспечить корректное завершение потоков

Код:
    Код программы представлен в файле prodcons.c, сборка и запуск файла осуществляется при помощи Makefile, конкретные параметры для запуска prodcons.c демонстрируются ниже:

Команды:
```bash
./prodcons -P 2 -C 2 -N 100000 -B 64
```
Результат:
```bash
P=2 C=2 N=100000 B=64 produced=100000 consumed=100000 sum=152499950000
```

Выводы:
   Программа producer-consumer работает корректно. Все 100000 элементов были произведены и потреблены без потерь. Сумма значений совпадает при повторных запусках,    что подтверждает отсутствие гонок данных и корректность синхронизации. Очередь с ограниченным размером (B=64) эффективно справляется с балансировкой между         производителями и потребителями.

### C) Где видны потоки
Цель: Показать свои потоки в системе и кратко прокомментируйте

Шаги/Решения:
- Получить PID какой-либо программы (например firefox).
- Выполнить предоставленную команду и изучить информацию, которую она предоставит.

Команда_0:
```pgrep firefox```

Результат_0:
```4649```

Команда_1:
```ps -L -p 4649 -o pid,tid,psr,pcpu,stat,comm | head -n 20 | cat
```

Результат_1:
```    PID     TID PSR %CPU STAT COMMAND
   4649    4649   3  1.4 Sl   firefox
   4649    4798   1  0.0 Sl   AsyncSi~lThread
   4649    4802   2  0.3 Sl   WaylandProxy
   4649    4803   2  0.0 Sl   pool-spawner
   4649    4804   1  0.0 Sl   gmain
   4649    4805   1  0.0 Sl   gdbus
   4649    4835   2  0.3 Sl   glean.dispatche
   4649    4837   2  0.5 Sl   IPC I/O Parent
   4649    4838   3  0.0 Sl   Timer
   4649    4839   0  0.0 Sl   Netlink Monitor
   4649    4840   2  0.1 Sl   Socket Thread
   4649    4841   2  0.2 Sl   IPDL Background
   4649    4849   3  0.0 Sl   dconf worker
   4649    4850   1  0.0 Sl   HTML5 Parser
   4649    4851   1  0.0 Sl   StyleThread#1
   4649    4852   3  0.0 Sl   StyleThread#2
   4649    4855   3  0.0 Sl   JS Watchdog
   4649    4866   2  0.0 Sl   Cache2 I/O
   4649    4867   0  0.0 Sl   Cookie
```

Команда_2:
```cat /proc/4649/status | grep Threads```

Результат_2:
```Threads:	89
```

Команда_3:
```ls -l /proc/4649/task | head -n 10 | cat```

Результат_3:
```итого 0
dr-xr-xr-x 7 teres teres 0 окт  7 00:13 4649
dr-xr-xr-x 7 teres teres 0 окт  7 00:13 4798
dr-xr-xr-x 7 teres teres 0 окт  7 00:13 4802
dr-xr-xr-x 7 teres teres 0 окт  7 00:13 4803
dr-xr-xr-x 7 teres teres 0 окт  7 00:13 4804
dr-xr-xr-x 7 teres teres 0 окт  7 00:13 4805
dr-xr-xr-x 7 teres teres 0 окт  7 00:13 4835
dr-xr-xr-x 7 teres teres 0 окт  7 00:13 4837
dr-xr-xr-x 7 teres teres 0 окт  7 00:13 4838
```

Команда_4:
```ls /proc/4649/task | wc -l```

Результат_4:
```85```

Выводы:
   Первая команда 'ps -L -p 4649' показала конкретные потоки процесса Firefox с их идентификаторами (TID), распределением по ядрам процессора (PSR), загрузкой CPU и состоянием. Видно, что разные потоки выполняют специализированные задачи: парсинг HTML, обработка стилей, сетевые операции, таймеры.

Вторая команда через '/proc/4649/status' показала общее количество потоков - 89. Третья команда вывела список директорий в '/proc/4649/task', где каждый подкаталог соответствует отдельному потоку. Четвертая команда подтвердила количество потоков через подсчет подкаталогов - 85 (небольшое расхождение с 89 может быть связано с динамическим созданием/уничтожением потоков во время выполнения команды).

Эксперимент наглядно демонстрирует, что современные приложения активно используют многопоточность для параллельной работы, и система предоставляет различные инструменты для наблюдения за потоками через procfs и утилиты командной строки.

## Ответы на вопросы
1. Процесс – это экземпляр выполняемой программы со своим пространством памяти и ресурсами, а поток – это минимальная единица выполнения внутри процесса, которая делит ресурсы и память с другими потоками одного процесса, но имеет собственные стеки и регистры. Увидеть поток можно: в ps - с помощью ключа -L (ps -L -p <PID>), в /proc - в каталогах /proc/<PID>/task/.
2. Race condition - возникает, когда несколько потоков одновременно обращаются к общим данным (например одновременно увеличивают счетчик - инкермент теряет обновление). Volatile отменяет некоторые оптимизации компилятора (например, кэширование в регистре) и указывает, что значение переменной может быть изменино извне, но он не обеспечивает атомарности операций или барьеров памяти.
3. mutex:основной инструмент защиты критических секций (участки кода, где происходит несколько операций с общими данными).
   condvar (условные переменные): используется вместе с mutex, когда потоку необходимо ждать выполнения некоторого условия. 
   semaphore: удобно для счетчиков ресурсов; более общий механизм, чем mutex, но не заменяет его. 
   атомики: используются для простых операций над одиночными переменными (короткие атомарные операции, например, декремент или инкремент).
4. Замедление происходит из-за накладных расходов (вызов функций синхронизации), блокировок и конкуренции. Способы уменьшения конкуренции: уменьшить размер критических секций, при возможности использовать более легкие примитивы (атомики), уменьшить частоту синхронизации.
5. Ложное совместное использование (false sharing) – это снижение производительности в многопоточных программах, когда несколько потоков работают с разными переменными, расположенными в одной кеш-линии процессора. Как избежать false sharing: сделать так, чтобы переменные, активно используемые разными потоками, гарантированно попадали в разные кэш-линии.

## Использование AI  
Помощь в:
- Написании кода на C,  
- Анализ данных, полученных после выполения программ,  
- формулировке ответов на вопросы.  

## Общие выводы
- Изучен механизм работы потоков в Linux.  
- Созданы 2 программы, хорошо показывающие работу потоков в операционной системе Linux.   
- Освоена работа с языком C для создания потоков.
- Изучен процесс появления race condition.

