# Лабораторная работа №4 Вариант 1

## Задание A: Анализ виртуальной памяти процесса

### Цель
- Написать программу, которая:
    - Выделяет память разными способами (стек, heap, mmap)
    - Выводит свою карту памяти из '/proc/self/maps'
    - Показывает VSZ, RSS, PSS, USS

### Шаги решения
1. Создаём программу на C.
2. Запускаем программу и сравниваем метрики до и после выделения памяти.
3. Используем системные утилиты для проверки.
4. Визуализируем карту памяти.
5. Ответить на вопросы для анализа.

### Решение

Запустим программу написанную в файле `memory_profiler.c`.

Вываод в консоли:
```
PID: 37022
```
Пока программа ожидает ввод `ENTER`, мы в новом терминале анализируем текущие показатели при помощи консольных команд:
```bash
ps -o pid,comm,vsz,rss -p 37022
cat /proc/37022/status | grep -E "^Vm"
cat /proc/37022/smaps_rollup
```
Вывод в консоли:
```
    PID COMMAND            VSZ   RSS
  37022 memory_profiler   2680  1312
VmPeak:	    2680 kB
VmSize:	    2680 kB
VmLck:	       0 kB
VmPin:	       0 kB
VmHWM:	    1312 kB
VmRSS:	    1312 kB
VmData:	     224 kB
VmStk:	     132 kB
VmExe:	       4 kB
VmLib:	    1748 kB
VmPTE:	      44 kB
VmSwap:	       0 kB
58985666c000-7ffe69fe3000 ---p 00000000 00:00 0                          [rollup]
Rss:                1536 kB
Pss:                 123 kB
Pss_Dirty:           100 kB
Pss_Anon:            100 kB
Pss_File:             23 kB
Pss_Shmem:             0 kB
Shared_Clean:       1424 kB
Shared_Dirty:          0 kB
Private_Clean:        12 kB
Private_Dirty:       100 kB
Referenced:         1536 kB
Anonymous:           100 kB
KSM:                   0 kB
LazyFree:              0 kB
AnonHugePages:         0 kB
ShmemPmdMapped:        0 kB
FilePmdMapped:         0 kB
Shared_Hugetlb:        0 kB
Private_Hugetlb:       0 kB
Swap:                  0 kB
SwapPss:               0 kB
Locked:                0 kB
```

После того, как выполнели bash-скрипт, переходим в начальное окно терминала и продолжаем работу программы введя `ENTER`.

Вывод в консоли:
```
=== After stack allocation ===
VmSize:	    9276 kB
VmRSS:	    2248 kB
Pss:                 345 kB
Pss_Dirty:           152 kB
Pss_Anon:            152 kB
Pss_File:            193 kB
Pss_Shmem:             0 kB
Private_Clean:       176 kB
Private_Dirty:       152 kB
Private_Hugetlb:       0 kB
SwapPss:               0 kB

=== After malloc (1 MB) ===
VmSize:	    9276 kB
VmRSS:	    2216 kB
Pss:                 349 kB
Pss_Dirty:           156 kB
Pss_Anon:            156 kB
Pss_File:            193 kB
Pss_Shmem:             0 kB
Private_Clean:       176 kB
Private_Dirty:       156 kB
Private_Hugetlb:       0 kB
SwapPss:               0 kB

=== After mmap (1 MB) ===
VmSize:	    9276 kB
VmRSS:	    2244 kB
Pss:                 349 kB
Pss_Dirty:           156 kB
Pss_Anon:            156 kB
Pss_File:            193 kB
Pss_Shmem:             0 kB
Private_Clean:       176 kB
Private_Dirty:       156 kB
Private_Hugetlb:       0 kB
SwapPss:               0 kB

=== /proc/self/maps ===
5e17736cd000-5e17736cf000 r--p 00000000 103:05 1990240                   /usr/bin/cat
5e17736cf000-5e17736d4000 r-xp 00002000 103:05 1990240                   /usr/bin/cat
5e17736d4000-5e17736d6000 r--p 00007000 103:05 1990240                   /usr/bin/cat
5e17736d6000-5e17736d7000 r--p 00008000 103:05 1990240                   /usr/bin/cat
5e17736d7000-5e17736d8000 rw-p 00009000 103:05 1990240                   /usr/bin/cat
5e17ab5f3000-5e17ab614000 rw-p 00000000 00:00 0                          [heap]
7d48e5a00000-7d48e5f75000 r--p 00000000 103:05 2006908                   /usr/lib/locale/locale-archive
7d48e6000000-7d48e6028000 r--p 00000000 103:05 1968544                   /usr/lib/x86_64-linux-gnu/libc.so.6
7d48e6028000-7d48e61b0000 r-xp 00028000 103:05 1968544                   /usr/lib/x86_64-linux-gnu/libc.so.6
7d48e61b0000-7d48e61ff000 r--p 001b0000 103:05 1968544                   /usr/lib/x86_64-linux-gnu/libc.so.6
7d48e61ff000-7d48e6203000 r--p 001fe000 103:05 1968544                   /usr/lib/x86_64-linux-gnu/libc.so.6
7d48e6203000-7d48e6205000 rw-p 00202000 103:05 1968544                   /usr/lib/x86_64-linux-gnu/libc.so.6
7d48e6205000-7d48e6212000 rw-p 00000000 00:00 0 
7d48e62e8000-7d48e630d000 rw-p 00000000 00:00 0 
7d48e631d000-7d48e631f000 rw-p 00000000 00:00 0 
7d48e631f000-7d48e6321000 r--p 00000000 00:00 0                          [vvar]
7d48e6321000-7d48e6323000 r--p 00000000 00:00 0                          [vvar_vclock]
7d48e6323000-7d48e6325000 r-xp 00000000 00:00 0                          [vdso]
7d48e6325000-7d48e6326000 r--p 00000000 103:05 1968541                   /usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2
7d48e6326000-7d48e6351000 r-xp 00001000 103:05 1968541                   /usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2
```
Теперь при помощи того же bash-скрипта получаем обновлённые показатели. И сравниваем прошлый результат с новым.

Вывод в консоли:
```
    PID COMMAND            VSZ   RSS
  37022 memory_profiler   4732  1500
VmPeak:	    4768 kB
VmSize:	    4732 kB
VmLck:	       0 kB
VmPin:	       0 kB
VmHWM:	    1500 kB
VmRSS:	    1500 kB
VmData:	    2276 kB
VmStk:	     132 kB
VmExe:	       4 kB
VmLib:	    1748 kB
VmPTE:	      48 kB
VmSwap:	       0 kB
58985666c000-7ffe69fe3000 ---p 00000000 00:00 0                          [rollup]
Rss:                1732 kB
Pss:                 128 kB
Pss_Dirty:           104 kB
Pss_Anon:            104 kB
Pss_File:             24 kB
Pss_Shmem:             0 kB
Shared_Clean:       1616 kB
Shared_Dirty:          0 kB
Private_Clean:        12 kB
Private_Dirty:       104 kB
Referenced:         1732 kB
Anonymous:           104 kB
KSM:                   0 kB
LazyFree:              0 kB
AnonHugePages:         0 kB
ShmemPmdMapped:        0 kB
FilePmdMapped:         0 kB
Shared_Hugetlb:        0 kB
Private_Hugetlb:       0 kB
Swap:                  0 kB
SwapPss:               0 kB
Locked:                0 kB
```
Выполнив сравнение результатов, можем заметить:
1. Рост VSZ (+2 MB)
    Это результат резервирования новых областей виртуальной памяти:
    - `malloc(1 MB)` → расширение кучи (heap) → рост VmData
    - `mmap(1 MB)` → добавление анонимного mmap-сегмента
    Обе операции увеличили виртуальный адресный диапазон, что и дало рост VSZ.
    VSZ отражает всю виртуальную память, в том числе не занятую физическими страницами.

2. RSS увеличился всего на ~188 kB
    Это значит:
    - Физически загруженные страницы (RAM) выросли немного.
    - Хотя 2 MB виртуальной памяти зарезервировано, ядро не выделяет физическую память сразу.
    - Страницы реально создаются лениво, при первом доступе (записи в память).
    `malloc` и `mmap` лишь резервируют виртуальное пространство, но не «тратят» оперативку, пока данные туда не записаны.

3. Pss и Private_ почти не изменились
    Это подтверждает, что новые области почти не тронули — их страницы не инициализировались и не стали «грязными» (Dirty).

4. VmData вырос с 224 → 2276 kB
    Это напрямую указывает на расширение heap-сегмента, куда попал malloc(1 MB).

Визуализация карты памяти:
```
Высокие адреса
┌───────────────────────────────┐
│ [vdso / vvar]                 │ ← быстрые системные вызовы
├───────────────────────────────┤
│ [ld-linux]                    │ ← динамический загрузчик
├───────────────────────────────┤
│ [libc]                        │ ← стандартная библиотека
├───────────────────────────────┤
│ [locale-archive]              │ ← база локализации
├───────────────────────────────┤
│ [heap]                        │ ← malloc / динамическая память
├───────────────────────────────┤
│ [Data / BSS]                  │ ← данные программы (rw)
├───────────────────────────────┤
│ [Code]                        │ ← исполняемый код `/usr/bin/cat`
└───────────────────────────────┘
Низкие адреса
```
Ответы на вопросы для анализа:
1. Почему VSZ (Virtual Size) намного больше RSS?
    - VSZ — это общий объём виртуально зарезервированных адресов.
    - RSS — это реально загруженные в физическую память страницы.
    - Когда процесс вызывает malloc или mmap, ядро резервирует виртуальное пространство, но физическая память выделяется лениво — только при первом доступе к странице.
    Поэтому VSZ может быть огромным (гигабайты), а RSS — маленьким.
2. Где находятся разные типы памяти в адресном пространстве? 
    Типичные диапазоны в /proc/self/maps (на 64-битной Linux):
```
Высокие адреса
┌───────────────────────────────┐
│ [Stack]                       │ 0x7fff...  
│ Вершина адресного пространства│  
├───────────────────────────────┤
│ [mmap region]                 │ 0x7f2c00000000+  
│ Анонимные mmap, библиотеки    │  
├───────────────────────────────┤
│ [Heap]                        │ 0x00600000–0x00800000  
│ malloc / динамическая память  │  
├───────────────────────────────┤
│ [Text / rodata]               │ 0x00400000–0x00600000  
│ Код программы                 │  
└───────────────────────────────┘
Низкие адреса
```

3. Как изменился RSS при выделении 1 MB через malloc vs mmap?

    - malloc(1 MB):
    Увеличивает heap, но физически страницы не выделяются, пока не произойдёт запись. Поэтому RSS растёт мало (на десятки килобайт).

    - mmap(1 MB):
    Создаёт новую анонимную область. Если в неё записать данные, RSS вырастет на ~1 MB, потому что страницы реально аллоцируются.

### Использование AI
В ходе выполнения лабораторной работы №4 Задание А был использован ИИ для следующих задач:
- Написание программы `memory_profiler.c`.
- Объяснение теориии по теме "Управление памятью и файловый ввод-вывод".
- Визуализация карты памяти.
- По подсказке ИИ из PR было исправлено несоответсвие имён фалов в Makefile, REPORT.md и файла с программой `memory_profiler.c`.

### Вывод
В ходе выполнения задания мы:

1. Написали и запустили программу memory_profiler.c, которая выделяет память различными способами: через стек, heap (malloc) и анонимный mmap.

2. С помощью /proc/self/status, /proc/self/smaps_rollup и системных утилит ps получили метрики виртуальной и физической памяти: VSZ, RSS, PSS, Private.

3. Проанализировали изменения метрик до и после выделения памяти:
    - VSZ увеличился на ~2 MB после malloc(1 MB) и mmap(1 MB). Это связано с резервированием виртуального адресного пространства, а не с реальным использованием RAM.
    - RSS изменился всего на десятки килобайт, поскольку физическая память выделяется лениво, только при обращении к страницам.
    - VmData вырос с 224 kB до 2276 kB, что указывает на расширение heap-сегмента.
    - PSS и Private практически не изменились, подтверждая, что новые области пока не стали «грязными» (Dirty) и фактически не занимают физическую память.

4. Визуализировали карту памяти ASCII-графикой, показав расположение ключевых сегментов:

5. Ответили на вопросы для анализа.

## Задание B: Memory Mapping vs Read/Write

### Цель
Сравнить производительность двух подходов к работе с большим файлом:
- Традиционный: open() + read()/write()
- Memory mapping: mmap()

### Шаги решения
1. Создадим тестовый файл размером 100MB.
2. Реализуем две функции:
    - `read_with_syscalls()` — читает файл через read(), считает сумму байтов
    - `read_with_mmap()` — читает через mmap(), считает сумму байтов
3. Замерим время выполнения каждого подхода.
4. Запустим несколько раз и усредним результаты.
5. Проанализируем page faults.

### Решение

Создаём тестовый файл размером 100MB:
```bash
dd if=/dev/urandom of=testfile.bin bs=1M count=100
```

В файле `file_analyzer.c` реализуем программу с функциями `read_with_syscalls()` и `read_with_mmap()`. Так же в этой программе будет реализованно измерение времени выполнения.

Запустим программу в фоне чтобы у нас был доступ к `/proc/[PID]/stat`:
```bash
./file_analyzer &
PID=$!
cat /proc/$PID/stat
wait $PID
```
Вывод в консоли:
```
[1] 48046
48046 (file_analyzer) R 8417 48046 8417 34818 48047 4194304 177 0 0 0 0 0 0 0 20 0 1 0 10793957 2609152 263 18446744073709551615 104684307943424 104684307945041 140732788399856 0 0 0 0 0 0 0 0 0 17 5 0 0 0 0 0 104684307955048 104684307955728 104685201887232 140732788404441 140732788404457 140732788404457 140732788408296 0
Sum = 13370767760
Time with read()/write(): 0.242736 seconds
Sum = 13370767760
Time with mmap(): 0.179010 seconds
[1]+  Done 
```
Для точности анализа выполняем запуск программы несколько раз.
Так же сделаем тест со сброшенным кэшем:
```bash
sudo sh -c 'echo 3 > /proc/sys/vm/drop_caches'
```
```
Таблица с временем выполнения и page faults:
+----------------------------+-------------------+-----------------+-----------------+-----------------------------+
| Метод                      | Время (среднее, с)| min_flt         | maj_flt         | Примечания                  |
+----------------------------+-------------------+-----------------+-----------------+-----------------------------+
| read()/write() (кэш полный)| 0.242             | 0               | 0               | Файл уже в RAM, кэш не      |
|                            |                   |                 |                 | сброшен                     |
| mmap() (кэш полный)        | 0.184             | 0               | 0               | Файл уже в RAM              |
| read()/write() (кэш сброшен)| 0.260            | 3               | 0               | Подгрузка страниц с диска   |
| mmap() (кэш сброшен)       | 0.187             | 3               | 0               | Лениво подгружает страницы  |
+----------------------------+-------------------+-----------------+-----------------+-----------------------------+
```
Объяснение разницы в скорости:
1. Системные вызовы vs прямой доступ к памяти:
    - `read()` использует системные вызовы на каждый блок, данные копируются из ядра в пользовательское пространство → больше накладных расходов.
    - `mmap()` отображает файл напрямую в память процесса → доступ к данным без лишних копий.

2. Подгрузка страниц (lazy loading):
    - `mmap()` подгружает страницы по мере обращения, тогда как `read()` читает данные блоками сразу.
    - Даже при первом обращении после очистки кэша mmap() эффективнее, т.к. ядро управляет подгрузкой страниц автоматически.

3. Page faults:
    - `min_flt` (мягкие) появляются, когда страницы подгружаются с диска после очистки кэша.
    - `maj_flt` (жёсткие) отсутствуют, т.к. файл помещается в RAM и нет необходимости обращаться к swap.
    - Мягкие page faults немного увеличивают время выполнения, но `mmap()` выигрывает за счёт меньших накладных расходов.

### Использование AI
В ходе выполнения лабораторной работы №4 Задание B был использован ИИ для следующих задач:
- Написание программы `file_analyzer.c`.
- Объяснение теориии по теме "Memory Mapping vs Read/Write".
- Визуализация таблицы сравнения.

### Вывод
В результате экспериментов установлено, что метод memory mapping (mmap) обеспечивает лучшую производительность по сравнению с традиционным подходом open() + read()/write() при работе с большими файлами (100 MB).

1. При наличии файлового кэша mmap() показывает время около 0.18 с, что примерно на 25 % быстрее, чем read() (~0.24 с).

2. После очистки кэша (drop_caches) оба метода выполняют дополнительные операции подгрузки страниц, но mmap() остаётся быстрее (0.19 с против 0.26 с).

2. Анализ /proc/[PID]/stat показал наличие мягких page faults (min_flt) при первом чтении с диска и отсутствие жёстких (maj_flt), что указывает на эффективное использование оперативной памяти без обращения к swap.

3. Таким образом:

    - mmap() выигрывает за счёт прямого отображения файла в адресное пространство процесса, отсутствия избыточных копирований данных между ядром и пользовательским пространством, а также ленивой (lazy) подгрузки страниц по мере доступа.

    - Метод read()/write(), в свою очередь, имеет большие накладные расходы на системные вызовы и копирование буферов, что делает его менее эффективным при работе с большими объёмами данных.

## Задание C

### Цель
- Изучить Page Faults в реальном времени

### Шаги решения
1. Написать программу в которой:
    1. Выделяет большой массив (например, 100 MB).
    2. Заполняет его данными с разными паттернами доступа: 
        - Последовательное заполнение 
        - Случайное заполнение
    3. Во время работы выводит количество minor/major page faults.
2. Проанализировать полученные данные.

### Решение

Запустим программу написанную в файле `page_faults.c`.

Вывод в консоли:
```
Minor faults: 78, Major faults: 0
Minor faults: 25682, Major faults: 0
Minor faults: 25683, Major faults: 0
```
Проанализируем полученный результат:
1. Почему при последовательном доступе больше page faults?
    Цикл:
    ```c
    for (size_t i = 0; i < size; i += 4096)
    arr[i] = 'A';
    ```
    Проходит по массиву страница за страницей (4096 байт = размер одной страницы).
    Каждая итерация затрагивает новую страницу, а значит — впервые обращается к памяти, которая ещё не выделена физически.

    На каждую такую страницу ОС вызывает один minor page fault.

    100 MB / 4 KB = примерно 25 600 страниц — ровно столько и видно в выводе:
    ```
    Minor faults: 78, Major faults: 0         ← до доступа
    Minor faults: 25682, Major faults: 0      ← после последовательного прохода
    ```
2. Почему major faults появляются (или нет)?
    ```c
    char *arr = malloc(size);
    for (size_t i = 0; i < size; i += 4096)
    arr[i] = 'A';
    ```
    - `malloc()` или `mmap(MAP_ANONYMOUS)` выделяет анонимную память — без связи с файлом.
    - При первом доступе ОС просто создаёт новую страницу в RAM (обнуляет её).
    - Никаких обращений к диску нет → `major fault` не нужен.

3. Как изменится поведение при повторном запуске?
    - Minor faults повторяются
    При каждом запуске программа вызывает:
    ```c
    arr = malloc(size);
    for (size_t i = 0; i < size; i += 4096)
    arr[i] = 'A';
    ```
    То есть — заново выделяет новый виртуальный диапазон (новые адреса в памяти).
    Эти адреса раньше не использовались текущим процессом, поэтому:
    - ОС снова лениво выделяет страницы при первом обращении
    - на каждую страницу возникает minor page fault,
    - и количество page faults остаётся примерно тем же (≈ 25 600).

    Major faults означают, что страница загружается с диска (например, файл через mmap, или страница подкачки).
    А программа:
    - работает только с анонимной памятью (через malloc),
    - не использует swap,
    - не читает файлы с диска.
    Поэтому при каждом запуске всё выделяется прямо из RAM — major faults не появляются вообще.

    Можно подумать, что ОС могла бы «запомнить» страницы и не выделять заново.
    Но это невозможно, потому что:
    - При завершении процесса ОС освобождает всю его память.
    - У нового процесса — новое адресное пространство (новая таблица страниц).
    - Старые страницы либо возвращаются в пул свободных страниц, либо очищаются.
    Поэтому при каждом запуске память выделяется с нуля, и minor page faults возникают заново — ровно так же, как в первый раз.

### Использование AI
В ходе выполнения лабораторной работы №4 Задание C был использован ИИ для следующих задач:
- Написание программы `page_faults.c`.
- Объяснение теориии по теме "Page Faults в реальном времени".
- По подсказке ИИ из PR добавлено инициализация rand() в page_faults.c: 
```c
srand(time(NULL));
```

### Вывод
В ходе выполнения лабораторной работы было изучено поведение системы при возникновении page faults (ошибок страниц) в реальном времени.

1. В процессе эксперимента была написана и запущена программа, выделяющая большой участок памяти (100 МБ) и заполняющая его разными способами:

    - последовательным доступом (по страницам),
    - случайным доступом к элементам массива.

2. При последовательном доступе наблюдалось значительное увеличение количества minor page faults — примерно на одну ошибку на каждую новую страницу памяти (около 25 600 faults). Это объясняется тем, что физические страницы RAM выделяются лениво, только при первом обращении к каждой странице виртуальной памяти.

3. Major page faults не возникали, так как программа использовала анонимную память (malloc), а не чтение данных с диска. Все страницы создавались сразу в оперативной памяти без участия ввода-вывода.

4. Повторные запуски программы показали одинаковое поведение, поскольку каждый процесс получает новое виртуальное адресное пространство, и все страницы снова выделяются заново при первом обращении. ОС не кэширует анонимную память после завершения процесса.