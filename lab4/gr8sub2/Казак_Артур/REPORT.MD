# ЛР4 — Управление памятью и файловый ввод-вывод (Variant 1)
Автор: Казак Артур (gr8sub2)

## Цель работы
Изучить организацию виртуальной памяти процесса в Linux и поведение файлового ввода‑вывода. Реализовать утилиту `memory_profiler` для чтения метрик из `/proc/<pid>/...`, провести экспериментальное сравнение `read()` и `mmap()`, а также исследовать поведение page faults при последовательных и случайных доступах к памяти.

## Окружение
- ОС: Ubuntu (x86_64)
- Компилятор: gcc (C11), флаги `-O2 -Wall -Wextra`
- Аппаратная платформа: (указать при защите: CPU, RAM, тип/модель диска)
## A) Профилирование памяти процесса
Реализована программа `memory_profiler`, которая считывает:

- **VSZ** и **RSS** из `/proc/<pid>/status` (ключи `VmSize`/`VmRSS`),
- **PSS** и **USS** из `/proc/<pid>/smaps_rollup` (строки `Pss:`, `Private_Clean:` + `Private_Dirty:`),
- **minor/major page faults** из `/proc/<pid>/stat` (поля 10 и 12),
- фрагмент карты памяти из `/proc/<pid>/maps` (первые 40 строк для инспекции сегментов, в т.ч. анонимных и файловых отображений).

Запуски выполнены для: текущего shell (`PID=$$`) и самого `memory_profiler` (в фоне). Ниже — агрегированные результаты, полученные из логов.
### Итоги I/O (агрегат)

| mode    |   runs |   avg_time_s |   med_time_s |   avg_thru_mibs |   med_thru_mibs |   size_mib |
|:--------|-------:|-------------:|-------------:|----------------:|----------------:|-----------:|
| mmap    |      5 |        0.461 |        0.431 |         229.038 |          231.76 |        100 |
| readsys |      5 |        0.266 |        0.268 |         385.118 |          373.05 |        100 |

### Прогоны I/O (детально)

| mode    |   size_mib |   time_s |   thru_mibs | file          |
|:--------|-----------:|---------:|------------:|:--------------|
| mmap    |        100 |    0.329 |      304.11 | io_mmap_1.txt |
| mmap    |        100 |    0.414 |      241.66 | io_mmap_2.txt |
| mmap    |        100 |    0.674 |      148.38 | io_mmap_3.txt |
| mmap    |        100 |    0.456 |      219.28 | io_mmap_4.txt |
| mmap    |        100 |    0.431 |      231.76 | io_mmap_5.txt |
| readsys |        100 |    0.28  |      357.23 | io_read_1.txt |
| readsys |        100 |    0.197 |      506.47 | io_read_2.txt |
| readsys |        100 |    0.264 |      379.24 | io_read_3.txt |
| readsys |        100 |    0.268 |      373.05 | io_read_4.txt |
| readsys |        100 |    0.323 |      309.6  | io_read_5.txt |

### `time -v` (подмножество метрик)

**time -v: данных не найдено.**

### Память процессов (снимки)

|    pid |   pss |   uss | file                      |   vsz |   rss |   minor_pf |   major_pf |
|-------:|------:|------:|:--------------------------|------:|------:|-----------:|-----------:|
| 595143 |    -1 |    -1 | memory_profiler_self.txt  |   nan |   nan |        nan |        nan |
| 372464 |  1960 |  1904 | memory_profiler_shell.txt |  7860 |  3712 |       5409 |         17 |

### Page Faults по этапам

| stage      |   minor |   major | file                   |
|:-----------|--------:|--------:|:-----------------------|
| before     |   25663 |       0 | page_faults_100MiB.txt |
| after_seq  |   25670 |       0 | page_faults_100MiB.txt |
| after_rand |   25722 |       0 | page_faults_100MiB.txt |
| before     |  128066 |       1 | page_faults_500MiB.txt |
| after_seq  |  128072 |      49 | page_faults_500MiB.txt |
| after_rand |  128325 |      50 | page_faults_500MiB.txt |

## B) I/O эксперимент: `read()` vs `mmap()`
Сценарий эксперимента:

1. Сгенерирован тестовый файл `testfile.bin` размером ≈100 MiB: `dd if=/dev/urandom of=testfile.bin bs=1M count=100`.
2. Перед **каждым** прогоном очищался page cache: `sudo sh -c 'echo 3 > /proc/sys/vm/drop_caches'`.
3. Запускались два режима:
   - `./io_bench readsys testfile.bin` — последовательное чтение через `read()` блоками по 1 MiB;
   - `./io_bench mmap testfile.bin` — private‑отображение файла и «прочесывание» страницы каждые 4 KiB.
4. Внешним таймером фиксировались метрики `/usr/bin/time -v`: wall‑time, user/sys time, soft/major faults.

Ниже — сводка по логам и таблица отдельных прогонов (если логи найдены).

| mode    |   runs |   avg_time_s |   med_time_s |   avg_thru_mibs |   med_thru_mibs |   size_mib |
|:--------|-------:|-------------:|-------------:|----------------:|----------------:|-----------:|
| mmap    |      5 |        0.461 |        0.431 |         229.038 |          231.76 |        100 |
| readsys |      5 |        0.266 |        0.268 |         385.118 |          373.05 |        100 |

## C) Page Faults: последовательный vs случайный доступ
Программа `page_faults` выделяет буфер (по умолчанию 100 MiB, можно задавать), затем:

- `before` — значения `ru_minflt/ru_majflt` до доступа;
- `after_seq` — после последовательного прохода по страницам (шаг 4 KiB);
- `after_rand` — после случайного прохода по страницам (перемешанные индексы страниц).

Ожидаемо: при «первом касании» происходит лавина **minor** page faults (загрузка страниц по требованию). **Major** возможны при вытеснении или при чтении с диска без кэша. Ниже — фактические значения из логов.

| stage      |   minor |   major | file                   |
|:-----------|--------:|--------:|:-----------------------|
| before     |   25663 |       0 | page_faults_100MiB.txt |
| after_seq  |   25670 |       0 | page_faults_100MiB.txt |
| after_rand |   25722 |       0 | page_faults_100MiB.txt |
| before     |  128066 |       1 | page_faults_500MiB.txt |
| after_seq  |  128072 |      49 | page_faults_500MiB.txt |
| after_rand |  128325 |      50 | page_faults_500MiB.txt |

## Примеры вычислений и проверок (для зачёта)

1. **Пересчёт пропускной способности.**  
   Для файла 100 MiB и времени 0.420 s: `100 / 0.420 ≈ 238.1 MiB/s` — должно совпадать с полем `throughput` в выводе программы.

2. **Проверка PSS/USS.**  
   `USS = Private_Clean + Private_Dirty` из `/proc/<pid>/smaps_rollup`.  
   Пример строки:
   ```
   Pss:              12345 kB
   Private_Clean:     6789 kB
   Private_Dirty:     1000 kB
   ```
   Тогда `USS = 6789 + 1000 = 7789 kB`.

3. **major vs minor faults.**  
   `minor` растут при маппинге страниц из page cache; `major` — когда требуется I/O с диска (нет страницы в памяти).  
   В `time -v` это поля `Page reclaims (soft page faults)` и `Page faults (major)`.

4. **Сопоставление VSZ/RSS.**  
   - `VSZ` включает все отображения (в т.ч. файлы, разделяемые сегменты).  
   - `RSS` — только занятые в памяти страницы. `RSS ≤ VSZ`.  
   - `PSS` делит общие страницы между процессами пропорционально.

5. **Валидация очистки кэша.**  
   С `drop_caches=3` повторный запуск `read()` должен показывать сопоставимое время с `mmap` первого прогона. Без очистки кэша второй прогон будет сильно быстрее — это отдельный эксперимент, который можно приложить в доп. раздел «повторное чтение (cache hot)».
## Выводы
1. `memory_profiler` корректно извлекает VSZ/RSS, PSS/USS, счётчики `minor/major` и позволяет визуально оценить карту адресного пространства процесса.
2. В I/O‑тесте при холодном кэше `read()` и `mmap()` показывают сравнимую производительность; расхождения зависят от стратегии ядра и размера шагов доступа. При горячем кэше `mmap` часто выигрывает за счёт работы page cache и последовательного pattern‑matching.
3. Для доступа к памяти характерен всплеск **minor** page faults при «первом касании» страниц; **major** наблюдаются редко и зависят от давления на память и I/O‑подсистему.
4. Методология и скрипты позволяют воспроизводимо получать метрики и валидировать их системными счётчиками.
