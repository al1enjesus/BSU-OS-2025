# Лабораторная 5 — Модули ядра Linux

Практика по разработке модулей ядра Linux: изучим архитектуру ядра, напишем простые модули, научимся взаимодействовать с user-space через `/proc` и `/sys`, создадим базовый character device.

## ⚠️ ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ О БЕЗОПАСНОСТИ

**Эта лабораторная работа ОБЯЗАТЕЛЬНО выполняется в виртуальной машине или изолированной среде!**

### Требования к окружению:
- ✅ **VirtualBox/VMware** с Ubuntu 22.04/24.04
- ✅ **Docker** контейнер с `--privileged` флагом
- ✅ **WSL2** (с осторожностью, не на всех системах работает)
- ❌ **НЕ на основной системе!** Ошибка в модуле может вызвать kernel panic

### Почему это важно:
- Код модуля выполняется в **kernel space** (привилегированный режим)
- Ошибка может привести к **краху всей системы** (kernel panic)
- Нет защиты памяти как в user-space
- Отладка сложнее

**Если у вас нет VM - сначала настройте её, потом приступайте к лабе!**

---

## Содержание
1. [Теория](#теория)
2. [Подготовка среды](#подготовка-среды)
3. [Структура сдачи](#структура-сдачи)
4. [Задания](#задания)
5. [Критерии зачёта](#критерии-зачёта)
6. [Вопросы для отчёта](#вопросы-для-отчёта)

---

## Теория

### 1. Что такое модули ядра?

#### 1.1 Определение

**Модуль ядра (Kernel Module)** — это фрагмент кода, который может быть **динамически загружен** и **выгружен** из ядра Linux во время работы системы без необходимости перезагрузки.

**Аналогия:** Представьте ядро как основу здания, а модули — как съёмные комнаты, которые можно добавлять и убирать без разрушения здания.

#### 1.2 Зачем нужны модули?

**До появления модулей:**
- Нужна функциональность? → Перекомпилируй всё ядро → Перезагрузка
- Каждая функция встроена в ядро → Огромный размер → Больше памяти

**С модулями:**
- Нужен драйвер? → `insmod driver.ko` → Сразу работает
- Не нужен? → `rmmod driver` → Память освобождена
- Ядро компактное, функциональность расширяемая

**Примеры использования:**
- Драйверы устройств (USB, сетевые карты, видеокарты)
- Файловые системы (ext4, btrfs, ntfs)
- Сетевые протоколы
- Системы безопасности (SELinux, AppArmor модули)

#### 1.3 Kernel Space vs User Space

```
┌─────────────────────────────────────────┐
│         User Space (непривилегированный) │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │  bash   │  │ firefox │  │   ls    │ │
│  └─────────┘  └─────────┘  └─────────┘ │
│         ↕ Системные вызовы              │
├─────────────────────────────────────────┤
│         Kernel Space (привилегированный)│
│  ┌─────────────────────────────────┐   │
│  │  Ядро Linux (vmlinuz)           │   │
│  │  ┌──────────┐  ┌──────────┐    │   │
│  │  │ Модуль 1 │  │ Модуль 2 │    │   │
│  │  └──────────┘  └──────────┘    │   │
│  └─────────────────────────────────┘   │
├─────────────────────────────────────────┤
│            Аппаратура (Hardware)        │
└─────────────────────────────────────────┘
```

**User Space (пользовательское пространство):**
- Обычные программы (bash, firefox, ваш код)
- Ограниченный доступ к ресурсам
- Крах программы → только эта программа умирает
- Можно использовать стандартную библиотеку C (malloc, printf и т.д.)

**Kernel Space (пространство ядра):**
- Ядро и модули
- Полный доступ ко всему (память, процессор, устройства)
- Крах кода → kernel panic → вся система падает
- **НЕТ** стандартной библиотеки C! (используются kernel API)

---

### 2. Анатомия простого модуля

#### 2.1 Минимальный модуль "Hello World"

```c
#include <linux/module.h>    // Обязательно для всех модулей
#include <linux/kernel.h>    // Для KERN_INFO, printk
#include <linux/init.h>      // Для макросов __init и __exit

// Функция инициализации (вызывается при insmod)
static int __init hello_init(void) {
    printk(KERN_INFO "Hello, kernel world!\n");
    return 0;  // 0 = успех, не-0 = ошибка
}

// Функция выгрузки (вызывается при rmmod)
static void __exit hello_exit(void) {
    printk(KERN_INFO "Goodbye, kernel world!\n");
}

// Регистрация функций init/exit
module_init(hello_init);
module_exit(hello_exit);

// Метаданные модуля
MODULE_LICENSE("GPL");                    // Лицензия (обязательно!)
MODULE_AUTHOR("Your Name");               // Автор
MODULE_DESCRIPTION("Simple Hello World"); // Описание
MODULE_VERSION("1.0");                    // Версия
```

#### 2.2 Разбор по частям

**1. Заголовочные файлы:**
```c
#include <linux/module.h>  // Базовый функционал модулей
#include <linux/kernel.h>  // printk, KERN_* макросы
#include <linux/init.h>    // __init, __exit макросы
```

**Важно:** Это НЕ стандартные заголовки C (`<stdio.h>` не работает в ядре!).

**2. Функция инициализации:**
```c
static int __init hello_init(void) {
    // Код, выполняющийся при загрузке модуля (insmod)
    return 0;  // 0 = успех
}
```

- `static` — функция видна только внутри этого файла
- `__init` — подсказка компилятору: эта память освободится после init
- Возвращаемое значение:
  - `0` — модуль загружен успешно
  - `< 0` — ошибка (модуль НЕ будет загружен)

**3. Функция выгрузки:**
```c
static void __exit hello_exit(void) {
    // Код, выполняющийся при выгрузке модуля (rmmod)
}
```

- `__exit` — память освободится при выгрузке
- Должна **очистить все ресурсы**, выделенные в init

**4. Регистрация:**
```c
module_init(hello_init);  // Какую функцию вызвать при insmod
module_exit(hello_exit);  // Какую функцию вызвать при rmmod
```

**5. Метаданные:**
```c
MODULE_LICENSE("GPL");  // ОБЯЗАТЕЛЬНО! Иначе ядро пометит модуль как "tainted"
MODULE_AUTHOR("...");
MODULE_DESCRIPTION("...");
```

**Важно про лицензию:**
- `"GPL"` — совместимо с лицензией ядра Linux
- Без этого модуль будет работать, но kernel будет "испорчен" (tainted)
- Некоторые функции ядра доступны только GPL модулям

---

### 3. Логирование в ядре: printk

#### 3.1 Чем printk отличается от printf?

**В user-space (printf):**
```c
printf("Hello %s\n", name);  // Вывод в stdout (терминал)
```

**В kernel-space (printk):**
```c
printk(KERN_INFO "Hello %s\n", name);  // Вывод в kernel log
```

**Основные отличия:**

| Аспект | printf | printk |
|--------|--------|--------|
| Куда выводит | stdout (терминал) | kernel log buffer |
| Где смотреть | Сразу в терминале | `dmesg`, `/var/log/kern.log` |
| Уровни важности | Нет | Есть (KERN_INFO, KERN_ERR и т.д.) |
| Может ли блокироваться | Да | Нет (должна работать всегда) |

#### 3.2 Уровни логирования

```c
printk(KERN_EMERG   "System is unusable");           // 0 - Аварийная
printk(KERN_ALERT   "Action must be taken");         // 1 - Требует действий
printk(KERN_CRIT    "Critical conditions");          // 2 - Критическая
printk(KERN_ERR     "Error conditions");             // 3 - Ошибка
printk(KERN_WARNING "Warning conditions");           // 4 - Предупреждение
printk(KERN_NOTICE  "Normal but significant");       // 5 - Заметка
printk(KERN_INFO    "Informational");                // 6 - Информация
printk(KERN_DEBUG   "Debug-level messages");         // 7 - Отладка
```

**Рекомендации:**
- `KERN_INFO` — для обычных сообщений (загрузка/выгрузка)
- `KERN_ERR` — для ошибок
- `KERN_DEBUG` — для отладки (может быть отключено)

#### 3.3 Как смотреть логи

**Способ 1: dmesg** (рекомендуется)
```bash
dmesg | tail -20              # Последние 20 строк
dmesg | grep "my_module"      # Поиск по модулю
dmesg -w                      # Watch mode (живой вывод)
dmesg -T                      # С человекочитаемым временем
```

**Способ 2: Файлы логов**
```bash
tail -f /var/log/kern.log     # Debian/Ubuntu
tail -f /var/log/messages     # RHEL/CentOS
```

**Способ 3: journalctl** (systemd)
```bash
journalctl -k -f              # Kernel logs в реальном времени
journalctl -k --since "1 hour ago"
```

---

### 4. Жизненный цикл модуля

```
┌─────────────┐
│   insmod    │  ← Загрузка модуля
│  mymod.ko   │
└──────┬──────┘
       │
       ↓
┌─────────────────────────────────┐
│   module_init() выполняется     │
│   - Инициализация ресурсов      │
│   - Регистрация /proc, /dev     │
│   - Выделение памяти            │
└──────┬──────────────────────────┘
       │
       ↓ (если return 0)
┌─────────────────────────────────┐
│   Модуль активен и работает     │
│   - Обрабатывает запросы        │
│   - Взаимодействует с user-space│
└──────┬──────────────────────────┘
       │
       ↓
┌─────────────┐
│    rmmod    │  ← Выгрузка модуля
│   mymod     │
└──────┬──────┘
       │
       ↓
┌─────────────────────────────────┐
│   module_exit() выполняется     │
│   - Освобождение ресурсов       │
│   - Отмена регистраций          │
│   - Очистка памяти              │
└─────────────────────────────────┘
```

**Важные правила:**

1. **Всё, что выделено в init, должно быть освобождено в exit**
   ```c
   // ❌ ПЛОХО - утечка памяти
   static int __init my_init(void) {
       buffer = kmalloc(1024, GFP_KERNEL);
       return 0;
   }
   static void __exit my_exit(void) {
       // Забыли освободить buffer!
   }

   // ✅ ХОРОШО
   static int __init my_init(void) {
       buffer = kmalloc(1024, GFP_KERNEL);
       return 0;
   }
   static void __exit my_exit(void) {
       kfree(buffer);  // Освобождаем
   }
   ```

2. **Если init вернула ошибку, exit НЕ вызывается**
   ```c
   static int __init my_init(void) {
       buffer = kmalloc(1024, GFP_KERNEL);
       if (!buffer)
           return -ENOMEM;  // Ошибка → exit НЕ вызовется

       if (register_something() < 0) {
           kfree(buffer);  // ← Освобождаем сами!
           return -EFAULT;
       }
       return 0;
   }
   ```

3. **Модуль нельзя выгрузить, если он используется**
   ```bash
   $ rmmod my_driver
   rmmod: ERROR: Module my_driver is in use
   ```
   Нужно сначала закрыть все процессы, использующие модуль.

---

### 5. Параметры модуля

Модули могут принимать параметры при загрузке.

#### 5.1 Объявление параметров

```c
#include <linux/moduleparam.h>

static int count = 1;                      // Переменная
module_param(count, int, 0644);           // Регистрация параметра
MODULE_PARM_DESC(count, "Number of iterations");  // Описание

static char *name = "world";
module_param(name, charp, 0644);          // charp = char pointer
MODULE_PARM_DESC(name, "Name to greet");
```

**Синтаксис:** `module_param(имя_переменной, тип, права_доступа)`

**Типы:**
- `int`, `uint` — целые числа
- `charp` — строка (char pointer)
- `bool` — логический
- `array` — массив

**Права доступа (permissions):**
- `0` — параметр не виден в sysfs
- `0444` — read-only
- `0644` — read-write (root может менять)

#### 5.2 Использование параметров

**При загрузке:**
```bash
insmod mymodule.ko count=5 name="Linux"
```

**В коде:**
```c
static int __init my_init(void) {
    printk(KERN_INFO "Count: %d, Name: %s\n", count, name);
    return 0;
}
```

**Просмотр через sysfs:**
```bash
ls /sys/module/mymodule/parameters/
cat /sys/module/mymodule/parameters/count
# Если права позволяют, можно изменить:
echo 10 > /sys/module/mymodule/parameters/count
```

---

### 6. Взаимодействие с user-space

Модули ядра нужны для взаимодействия с пользовательскими программами. Основные способы:

#### 6.1 /proc файловая система

**ProcFS** — виртуальная ФС для экспорта информации из ядра в user-space.

**Примеры существующих proc файлов:**
```bash
cat /proc/cpuinfo      # Информация о CPU
cat /proc/meminfo      # Информация о памяти
cat /proc/version      # Версия ядра
```

**Создание своего /proc файла в модуле:**

```c
#include <linux/proc_fs.h>
#include <linux/uaccess.h>

static struct proc_dir_entry *proc_file;

// Функция чтения (вызывается при cat /proc/myfile)
static ssize_t my_read(struct file *file, char __user *ubuf,
                       size_t count, loff_t *ppos) {
    char buf[256];
    int len;

    if (*ppos > 0)  // Уже читали
        return 0;

    len = snprintf(buf, sizeof(buf), "Hello from kernel!\n");

    // Копирование из kernel-space в user-space
    if (copy_to_user(ubuf, buf, len))
        return -EFAULT;

    *ppos = len;
    return len;
}

static const struct proc_ops my_proc_ops = {
    .proc_read = my_read,
};

static int __init my_init(void) {
    proc_file = proc_create("myfile", 0444, NULL, &my_proc_ops);
    if (!proc_file)
        return -ENOMEM;
    return 0;
}

static void __exit my_exit(void) {
    proc_remove(proc_file);
}
```

**Использование:**
```bash
cat /proc/myfile
# Hello from kernel!
```

**Важно:**
- `copy_to_user()` — копирование данных из ядра в user-space
- `copy_from_user()` — копирование из user-space в ядро
- Нельзя просто использовать указатели user-space в ядре!

#### 6.2 /sys файловая система (sysfs)

**SysFS** — современный способ экспорта атрибутов устройств и параметров.

**Философия sysfs:**
- Один файл = одно значение
- Простой текстовый формат
- Используется для конфигурации устройств

**Пример:**
```bash
cat /sys/class/net/eth0/address    # MAC адрес
cat /sys/block/sda/size            # Размер диска
```

**Создание sysfs атрибута:** (упрощённый пример)
```c
#include <linux/sysfs.h>
#include <linux/kobject.h>

static int my_value = 0;

// Функция чтения атрибута
static ssize_t my_show(struct kobject *kobj, struct kobj_attribute *attr,
                       char *buf) {
    return sprintf(buf, "%d\n", my_value);
}

// Функция записи атрибута
static ssize_t my_store(struct kobject *kobj, struct kobj_attribute *attr,
                        const char *buf, size_t count) {
    sscanf(buf, "%d", &my_value);
    return count;
}

static struct kobj_attribute my_attr = __ATTR(value, 0664, my_show, my_store);
```

#### 6.3 Character Device (/dev)

**Character Device** — устройство, с которым можно работать как с файлом.

**Примеры:**
- `/dev/null` — "чёрная дыра" (всё, что пишешь, исчезает)
- `/dev/zero` — источник нулей
- `/dev/random` — генератор случайных чисел
- `/dev/ttyS0` — последовательный порт

**Базовая структура:**
```c
#include <linux/fs.h>
#include <linux/cdev.h>

static int dev_open(struct inode *inode, struct file *file) {
    printk(KERN_INFO "Device opened\n");
    return 0;
}

static int dev_release(struct inode *inode, struct file *file) {
    printk(KERN_INFO "Device closed\n");
    return 0;
}

static ssize_t dev_read(struct file *file, char __user *buf,
                        size_t len, loff_t *off) {
    // Чтение из устройства
    return 0;
}

static ssize_t dev_write(struct file *file, const char __user *buf,
                         size_t len, loff_t *off) {
    // Запись в устройство
    return len;
}

static struct file_operations fops = {
    .open = dev_open,
    .release = dev_release,
    .read = dev_read,
    .write = dev_write,
};
```

**Регистрация устройства:**
```c
static dev_t dev_num;
static struct cdev my_cdev;

static int __init my_init(void) {
    // Выделить major/minor номера
    alloc_chrdev_region(&dev_num, 0, 1, "mydevice");

    // Инициализировать cdev
    cdev_init(&my_cdev, &fops);
    cdev_add(&my_cdev, dev_num, 1);

    return 0;
}
```

---

### 7. Выделение памяти в ядре

**В user-space:** `malloc()`, `free()`
**В kernel-space:** `kmalloc()`, `kfree()`

#### 7.1 kmalloc / kfree

```c
#include <linux/slab.h>

// Выделение
void *ptr = kmalloc(size, GFP_KERNEL);
if (!ptr) {
    printk(KERN_ERR "kmalloc failed\n");
    return -ENOMEM;
}

// Использование
memset(ptr, 0, size);

// Освобождение
kfree(ptr);
```

**Флаги GFP (Get Free Pages):**

- `GFP_KERNEL` — обычное выделение (может спать)
- `GFP_ATOMIC` — атомарное выделение (НЕ может спать, для прерываний)
- `GFP_USER` — для user-space данных

**Важно:** Всегда проверяйте возвращаемое значение!

#### 7.2 Разница с user-space

| User-space | Kernel-space |
|------------|--------------|
| `malloc()` | `kmalloc()` |
| `free()` | `kfree()` |
| Виртуальная память | Физическая память напрямую |
| Swap возможен | Swap НЕТ |
| Безопасно ошибаться | Ошибка = kernel panic |

---

### 8. Безопасность и лучшие практики

#### 8.1 Что может пойти не так

**1. Kernel Panic** — аналог "синего экрана смерти"
```
Kernel panic - not syncing: Fatal exception in interrupt
```
Причины:
- Обращение к NULL указателю
- Выход за границы массива
- Разыменование невалидного указателя
- Бесконечный цикл в обработчике прерывания

**2. Kernel Tainted** — "испорченное" ядро
```bash
$ cat /proc/sys/kernel/tainted
1
```
Причины:
- Загружен проприетарный модуль
- Модуль без правильной лицензии
- Принудительная загрузка модуля

**3. Memory Leaks** — утечки памяти
В ядре нет garbage collector! Утечка = потеря памяти навсегда (до reboot).

#### 8.2 Правила безопасного кода

**✅ DO (Делать):**

1. **Всегда проверяйте возвращаемые значения**
   ```c
   ptr = kmalloc(size, GFP_KERNEL);
   if (!ptr)
       return -ENOMEM;
   ```

2. **Освобождайте все ресурсы в exit**
   ```c
   static void __exit my_exit(void) {
       proc_remove(proc_file);
       kfree(buffer);
       unregister_chrdev(major, "mydev");
   }
   ```

3. **Используйте правильные функции копирования**
   ```c
   // ✅ ПРАВИЛЬНО
   copy_to_user(user_buf, kernel_buf, size);

   // ❌ НЕПРАВИЛЬНО
   memcpy(user_buf, kernel_buf, size);  // ОПАСНО!
   ```

4. **Проверяйте границы**
   ```c
   if (offset >= MAX_SIZE)
       return -EINVAL;
   ```

**❌ DON'T (Не делать):**

1. **НЕ используйте стандартную библиотеку C**
   ```c
   // ❌ НЕ работает в ядре
   printf("Hello");
   malloc(100);
   fopen("file.txt", "r");
   sleep(1);
   ```

2. **НЕ используйте числа с плавающей точкой**
   ```c
   // ❌ НЕТ FPU в kernel-space
   float x = 3.14;
   ```

3. **НЕ делайте бесконечные циклы**
   ```c
   // ❌ ОПАСНО - может заблокировать систему
   while (1) {
       // ...
   }
   ```

4. **НЕ обращайтесь к user-space указателям напрямую**
   ```c
   // ❌ ОПАСНО
   char *user_ptr = (char *)arg;
   printk("%s", user_ptr);  // Может вызвать kernel panic!

   // ✅ ПРАВИЛЬНО
   char kernel_buf[256];
   copy_from_user(kernel_buf, user_ptr, sizeof(kernel_buf));
   ```

#### 8.3 Отладка модулей

**1. printk — ваш лучший друг**
```c
printk(KERN_DEBUG "Entering function %s\n", __func__);
printk(KERN_DEBUG "Value: %d, Pointer: %p\n", val, ptr);
```

**2. Проверка в dmesg**
```bash
dmesg -w  # Смотрим логи в реальном времени
```

**3. Информация о модулях**
```bash
lsmod                    # Список загруженных модулей
modinfo mymodule.ko      # Информация о модуле
cat /proc/modules        # Детали модулей
```

**4. Если произошёл kernel panic**
- Виртуальная машина позволяет сделать snapshot/rollback
- Логи могут быть в `/var/log/kern.log` (если система загрузилась)
- Используйте `dmesg` сразу после reboot

---

## Подготовка среды

### Вариант 1: VirtualBox/VMware (рекомендуется)

```bash
# 1. Установить Ubuntu 22.04/24.04 в VM
# 2. Внутри VM установить необходимые пакеты:

sudo apt update
sudo apt install -y build-essential linux-headers-$(uname -r) \
     kmod dkms git vim

# Проверить версию ядра
uname -r

# Проверить наличие headers
ls /lib/modules/$(uname -r)/build
```

### Вариант 2: Docker (продвинутый)

```bash
# Запуск привилегированного контейнера
docker run -it --privileged \
    -v $(pwd):/workspace \
    ubuntu:22.04 bash

# Внутри контейнера
apt update
apt install -y build-essential linux-headers-generic kmod
```

### Вариант 3: WSL2 (осторожно!)

```bash
# WSL2 может НЕ поддерживать загрузку модулей на некоторых системах
# Проверьте:
uname -r
ls /lib/modules/$(uname -r)/

# Если headers нет:
# Вам нужно скомпилировать кастомное ядро WSL2 или использовать VM
```

---

## Структура сдачи

```
lab5/
  gr<группа>sub<подгруппа>/
    ФАМИЛИЯ_ИМЯ/
      REPORT.MD          # обязательный отчёт
      src/
        hello_module.c
        proc_module.c
        char_device.c
      Makefile           # для сборки модулей
      screenshots/       # dmesg, lsmod и т.д.
      logs/              # логи экспериментов
```

В `lab5/samples/` лежат скелеты для начала работы.

---

## Задания

### Правило выбора варианта
- **Нечётный номер по списку** → Вариант 1
- **Чётный номер по списку** → Вариант 2

---

## Вариант 1 (нечётные номера)

### Задание A: Hello World модуль

Создайте простой модуль, который:
1. При загрузке (`insmod`) выводит "Hello from [ВАШ_ИМЯ] module!"
2. При выгрузке (`rmmod`) выводит "Goodbye from [ВАШ_ИМЯ] module!"
3. Принимает параметр `message` (строка)
4. Если параметр задан, выводит его вместо дефолтного сообщения

**Требования:**
- Использовать `printk` с `KERN_INFO`
- Правильные метаданные (`MODULE_LICENSE`, `MODULE_AUTHOR`)
- `module_param` для параметра

**Проверка:**
```bash
make
sudo insmod hello_module.ko
dmesg | tail -5

sudo insmod hello_module.ko message="Custom greeting"
dmesg | tail -5

sudo rmmod hello_module
dmesg | tail -5
```

---

### Задание B: /proc файл с информацией

Создайте модуль, который создаёт файл `/proc/student_info` с информацией:
- Ваше имя
- Группа и подгруппа
- Текущее время загрузки модуля (в секундах с boot, используйте `jiffies`)
- Счётчик обращений к файлу

**Требования:**
- Использовать `proc_create()` и `proc_remove()`
- Реализовать функцию чтения (`proc_read`)
- Счётчик должен увеличиваться при каждом `cat /proc/student_info`

**Пример вывода:**
```bash
$ cat /proc/student_info
Name: Ivanov Ivan
Group: 6, Subgroup: 1
Module loaded at: 12345 jiffies
Read count: 1

$ cat /proc/student_info
Name: Ivanov Ivan
Group: 6, Subgroup: 1
Module loaded at: 12345 jiffies
Read count: 2
```

**Подсказка:** Используйте глобальную переменную для счётчика.

---

### Задание C: Простой character device

Создайте character device `/dev/mychardev`, который:
1. Можно открыть/закрыть
2. При записи сохраняет данные в kernel buffer (максимум 1024 байта)
3. При чтении возвращает сохранённые данные
4. Выводит в `dmesg` когда устройство открывается/закрывается

**Требования:**
- Использовать `alloc_chrdev_region()`, `cdev_init()`, `cdev_add()`
- Реализовать `open`, `release`, `read`, `write`
- Использовать `copy_to_user()` и `copy_from_user()`

**Проверка:**
```bash
sudo insmod chardev_module.ko
sudo mknod /dev/mychardev c <MAJOR> 0  # major из dmesg

echo "Hello" > /dev/mychardev
cat /dev/mychardev
# Hello

dmesg | tail -10
```

---

## Вариант 2 (чётные номера)

### Задание A: Hello World модуль

То же, что в Варианте 1.

---

### Задание B: /proc файл с записью

Создайте модуль, который создаёт файл `/proc/my_config` с возможностью записи:
- По умолчанию содержит строку "default"
- При записи (`echo "text" > /proc/my_config`) сохраняет новое значение
- При чтении (`cat /proc/my_config`) возвращает текущее значение
- Максимальная длина строки: 256 символов

**Требования:**
- Использовать `proc_create()` с правами 0666
- Реализовать и `proc_read` и `proc_write`
- Использовать `copy_from_user()` для записи

**Пример:**
```bash
$ cat /proc/my_config
default

$ echo "new value" > /proc/my_config
$ cat /proc/my_config
new value
```

---

### Задание C: /proc файл со статистикой системы

Создайте модуль, который создаёт файл `/proc/sys_stats` с информацией:
- Количество запущенных процессов (из `/proc`)
- Используемая память (можно приблизительно через `si_meminfo`)
- Uptime системы (через `jiffies_to_msecs()`)

**Требования:**
- Реализовать функцию чтения
- Форматировать вывод в читаемом виде

**Пример вывода:**
```bash
$ cat /proc/sys_stats
Processes: 234
Memory Used: 2048 MB
System Uptime: 3600 seconds
```

**Подсказка:** Для подсчёта процессов можно использовать простой счётчик из `for_each_process()` (если доступно) или просто вывести фиктивные данные для демонстрации.

---

## Критерии зачёта

### Обязательно:
- ✅ Выполнены задания A, B, C своего варианта
- ✅ Код компилируется без ошибок и предупреждений
- ✅ Модули корректно загружаются и выгружаются
- ✅ Нет kernel panic при работе с модулями
- ✅ Отчёт `REPORT.MD` заполнен полностью
- ✅ Скриншоты `dmesg`, `lsmod`, `/proc` файлов
- ✅ Даны ответы на все вопросы

### Для бонусных баллов (+0.5-1):
- Добавлена обработка ошибок во всех функциях
- Реализовано дополнительное задание (например, sysfs атрибут)
- Использованы расширенные возможности (ioctl для character device)
- Подробный анализ в отчёте

---

## Вопросы для отчёта (обязательно)

### Базовые понятия:
1. Что такое модуль ядра и зачем он нужен?
2. Чем отличается kernel-space от user-space?
3. Что произойдёт, если в модуле обратиться к NULL указателю?
4. Почему нельзя использовать `printf()` в модуле ядра?
5. Что такое kernel panic и как его избежать?

### Жизненный цикл модуля:
6. Какие функции вызываются при `insmod` и `rmmod`?
7. Что должна делать функция `module_exit()`?
8. Что происходит, если `module_init()` возвращает ошибку?
9. Можно ли выгрузить модуль, если он используется?

### Логирование и отладка:
10. Чем `printk()` отличается от `printf()`?
11. Какие уровни логирования существуют в ядре?
12. Как посмотреть логи модуля?
13. Что означает "tainted kernel"?

### Память:
14. Чем `kmalloc()` отличается от `malloc()`?
15. Что такое флаги GFP и зачем они нужны?
16. Что произойдёт, если не освободить память в `module_exit()`?
17. Почему нельзя использовать user-space указатели напрямую в ядре?

### Взаимодействие с user-space:
18. Что такое `/proc` и для чего он используется?
19. Что такое `/sys` (sysfs) и чем отличается от procfs?
20. Зачем нужны функции `copy_to_user()` и `copy_from_user()`?
21. Что такое character device и как он работает?

### Параметры и метаданные:
22. Как передать параметры модулю при загрузке?
23. Зачем нужен `MODULE_LICENSE()`?
24. Что произойдёт, если не указать лицензию?

### Безопасность:
25. Какие основные правила безопасного кода в ядре?
26. Можно ли использовать бесконечный цикл в модуле?
27. Почему в ядре нет FPU операций?
28. Что делать, если модуль вызвал kernel panic?

### Практические вопросы:
29. Как узнать, какие модули загружены в системе?
30. Как получить информацию о модуле (версия, параметры)?

---

## Полезные команды

### Работа с модулями:
```bash
# Загрузка модуля
sudo insmod module.ko
sudo insmod module.ko param=value

# Выгрузка модуля
sudo rmmod module_name

# Список загруженных модулей
lsmod
lsmod | grep mymodule

# Информация о модуле
modinfo module.ko

# Детали из /proc
cat /proc/modules
```

### Просмотр логов:
```bash
# Последние сообщения ядра
dmesg | tail -20

# В реальном времени
dmesg -w

# С временными метками
dmesg -T

# Поиск по модулю
dmesg | grep module_name

# Очистка dmesg (для тестов)
sudo dmesg -C
```

### Работа с устройствами:
```bash
# Создать device node
sudo mknod /dev/mydevice c <major> <minor>

# Посмотреть major/minor номера
ls -l /dev/

# Права доступа
sudo chmod 666 /dev/mydevice

# Удалить device node
sudo rm /dev/mydevice
```

### Отладка:
```bash
# Проверка "tainted" статуса
cat /proc/sys/kernel/tainted

# Информация о ядре
uname -r
cat /proc/version

# Проверка headers
ls /lib/modules/$(uname -r)/build
```

---

## Справочные ресурсы

### Документация:
```bash
# Man pages
man insmod
man rmmod
man lsmod
man modinfo

# Kernel documentation
/usr/src/linux-headers-$(uname -r)/Documentation/
```

### Онлайн ресурсы:
- The Linux Kernel Module Programming Guide: https://sysprog21.github.io/lkmpg/
- Linux kernel documentation: https://www.kernel.org/doc/html/latest/
- LWN.net: https://lwn.net/ (отличные статьи про ядро)

### Заголовочные файлы (для справки):
```c
#include <linux/module.h>      // MODULE_*, module_init/exit
#include <linux/kernel.h>      // printk, KERN_*
#include <linux/init.h>        // __init, __exit
#include <linux/moduleparam.h> // module_param
#include <linux/proc_fs.h>     // proc_create, proc_remove
#include <linux/uaccess.h>     // copy_to_user, copy_from_user
#include <linux/fs.h>          // file_operations, alloc_chrdev_region
#include <linux/cdev.h>        // cdev_*
#include <linux/slab.h>        // kmalloc, kfree
#include <linux/jiffies.h>     // jiffies
```

---

## Безопасность - ещё раз!

### ⚠️ ПЕРЕД НАЧАЛОМ:

1. ✅ Работаете в виртуальной машине?
2. ✅ Есть snapshot/бэкап?
3. ✅ Понимаете, что код в ядре может сломать систему?
4. ✅ Готовы к kernel panic и reboot?

### 🚨 Если произошёл kernel panic:

1. **Не паникуйте сами** (простите за каламбур)
2. Перезагрузите VM
3. Модуль **НЕ** загрузится автоматически после reboot
4. Проверьте код, исправьте ошибку
5. Попробуйте снова

### ✅ Признаки хорошего модуля:

- Загружается без ошибок
- Выгружается без ошибок
- Не оставляет следов в памяти
- Логи понятные и информативные
- Можно загрузить и выгрузить несколько раз

---

**Удачи в разработке модулей ядра! Будьте осторожны и внимательны!** 🐧🔧
