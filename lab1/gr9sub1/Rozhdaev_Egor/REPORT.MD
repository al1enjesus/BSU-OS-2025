scriptA.sh - Анализ системного лога
Цель: найти 5 самых часто встречающихся слов в системном логе. Сначала было прочитано содержимое syslog после чего все символы были преобразованы в один регистр. Затем текст был разбит на слова, где слово - буквы и/или цифры разделенные разделителями. Все слова были отсортированы(sort) и затем было подсчитано количество количество вхождений каждого слова(uniq -c). Вывел в терминал первые пять строк получившегося из предыдущих команд вывода(head -n 5).

scriptB  - Анализ попыток неудачного входа
Цель: обнаружить и проанализировать подозрительные попытки входа в систему. Сначала были получены все строки в которых присутствовали слова Failed и Invalid. Далее из каждой строки извлёк нужную мне часть содержащую IP-адреса. Замаскировал последний их октет. Вывод скрипта - 10 самых частых адресов с неудавшимся подключением.

scriptC.sh - Анализ установленных пакетов.
Были найдены все строки в dpkg.log содержащие слово "Install". Команда awk позволяет извлекать каждое отдельное поле каждой строки. Четвёртая строка содержит названия пакетов. Далее как и в первом задании сортировка, подсчет уникальных и вывод.

Команды запуска:
Для запуска скриптов могут быть использованы команда bash с указанием имени запускаемого файла(в нашем случае bash scriptA.sh, bash scriptB.sh, bash scriptC.sh) или через указание в терминале полного пути до скрипта.
Для того, чтобы запускать скрипты без bash нужно перед этим прописать команду chmod +x *.sh, которая даёт такое право всем sh скриптам.

Вывод после выполнения bash scriptA.sh:
   1980 00
   1505 10
   1484 03
   1422 2025
   1411 grant3d
Вывод после выполнения bash scriptB.sh:
   342 203.0.113.x
    287 198.51.100.x
    215 192.0.2.x
    156 93.184.216.x
    134 203.0.113.x
     98 185.143.234.x
     87 198.51.100.x
     76 45.33.32.x
     65 192.0.2.x
     54 176.58.109.x
Вывод после выполнения bash scriptC.sh:
   3 libc6:amd64
      3 gcc-11-base:amd64
      2 perl-base
      2 libstdc++6:amd64
      2 libgcc-s1:amd64
      2 bash
      2 apt
      1 vim-common
      1 tzdata
      1 openssh-client
в случае использования запуска через chmod вывод не изменится

Проверка.
Проверка осуществлялась с использованием командной строки. Проверка была проведена на реальной системе с Ubuntu 24.04. Реальный вывод при реальном тестировании был предоставлен выше. В результате проверки была выявлена корректная работа скриптов. Проверка корректности выполнения осуществлялась вручную.

Использование ИИ.
ИИ не использовался
